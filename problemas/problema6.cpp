#include <iostream>
using namespace std;
/*
int main()


{
    int a,b,c,suma=0,mul1,mul2;
    cout<<"ingrese el primer numero a encontrar multiplos"<<endl;
    cin>>a;
    cout<<"ingrese el segundo numero a encontrar multiplos"<<endl;
    cin>>b;
    cout<<"ingrese el tercer numero que es mayor a los  multiplos"<<endl;
    cin>>c;
    for(int i=1;i*a<c or i*b<c;i++){


        if(i*a<c)
            mul1=i*a;
        if(i*b<c)
            mul2=i*b;

        if(mul1/2 !=mul2 and i*a<c){
            suma=suma+mul1;
            cout<<mul1<<"+";
        }
        mul1=1;
        mul2=1;
    }
    for(int i=1;i*a<c or i*b<c;i++){


        if(i*a<c)
            mul1=i*a;
        if(i*b<c)
            mul2=i*b;

        if(mul2/2 !=mul1 && i*b<c && (i*b)%a!=0){
            suma=suma+mul2;
           cout<<mul2<<"+";
        }
        mul1=1;
        mul2=1;
    }
    cout<<"="<<suma<<endl;

     return 0;
}
*/
